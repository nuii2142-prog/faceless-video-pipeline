# ── secrets — NEVER commit ─────────────────────────────
.env
*.key

# ── python env / cache ─────────────────────────────────
ml-env/
__pycache__/
*.pyc

# ── local tools, models, scratch ───────────────────────
LOGO/
test_output/
data/
scratch_tt/
.playwright-mcp/
F5 TTS/

# ── PERSONAL media — this repo is PUBLIC, never publish these ──
# (voice recordings are voice-clone source material; photos are private)
voice over/
farm view Ref/
*.jpeg

# ── media: large and/or copyrighted ─────────────────────
*.mp3
*.wav
*.mp4
SFX/

# ── per-video outputs: the channel's content, not the pipeline ──
output/
Test voice/
markitdown/
notes/*
!notes/_template.md

# (showcase/ images are intentionally NOT ignored — curated portfolio shots)

# ── idea/topic planning: content, not pipeline ──
ideas/
